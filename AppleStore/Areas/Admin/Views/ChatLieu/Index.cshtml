@model IEnumerable<AppleStore.Model.ChatLieu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPageAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenChatLieu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenChatLieu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaChatLieu }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaChatLieu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaChatLieu })
        </td>
    </tr>
}

</table>
